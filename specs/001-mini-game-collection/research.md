# Research: 小游戏集合平台

**Feature**: 001-mini-game-collection  
**Date**: 2025-12-10

## 研究任务

### 1. Unity 多场景管理最佳实践

**Decision**: 使用 Unity SceneManager 进行场景切换，每个游戏独立场景

**Rationale**:
- 场景隔离：每个游戏的资源和逻辑独立，便于管理和调试
- 内存管理：切换场景时自动卸载旧场景资源，避免内存泄漏
- 团队协作：不同游戏可以并行开发，减少合并冲突

**Alternatives considered**:
- 单场景 + 动态加载：复杂度高，资源管理困难
- Additive Scene Loading：适合大型项目，对小游戏集合过于复杂

**Implementation**:
```csharp
// 场景切换示例
SceneManager.LoadScene("MainMenu");
SceneManager.LoadScene("Match3Game");
SceneManager.LoadScene("GomokuGame");
```

---

### 2. 游戏通用接口设计

**Decision**: 定义 `IGame` 接口和 `GameBase` 抽象基类

**Rationale**:
- 统一生命周期：所有游戏遵循相同的初始化、暂停、恢复、结束流程
- 便于扩展：新增游戏只需实现接口，无需修改主菜单逻辑
- 数据一致性：统一的分数和进度保存接口

**Interface Design**:
```csharp
public interface IGame
{
    string GameId { get; }
    string GameName { get; }
    void Initialize();
    void Pause();
    void Resume();
    void Restart();
    int GetScore();
    int GetHighScore();
}
```

---

### 3. 五子棋 AI 算法

**Decision**: 使用 Minimax 算法 + Alpha-Beta 剪枝，搜索深度 3-4 层

**Rationale**:
- 性能：在移动设备上 5 秒内完成决策
- 难度适中：对休闲玩家有挑战但不会太难
- 实现简单：经典算法，易于调试和优化

**Alternatives considered**:
- 纯随机：太简单，无挑战性
- 蒙特卡洛树搜索 (MCTS)：实现复杂，对五子棋效果不如 Minimax
- 深度学习：需要训练数据和模型，过于复杂

**Evaluation Function**:
- 五连：胜利 (∞)
- 活四：高分 (10000)
- 冲四：中高分 (1000)
- 活三：中分 (100)
- 眠三：低分 (10)

---

### 4. 本地数据持久化

**Decision**: 使用 Unity PlayerPrefs + JSON 序列化

**Rationale**:
- 简单可靠：Unity 内置 API，跨平台兼容
- 无需权限：不需要文件系统访问权限
- 足够容量：小游戏数据量小，PlayerPrefs 完全够用

**Alternatives considered**:
- SQLite：过于复杂，不适合简单游戏数据
- 文件系统：需要处理权限和路径问题
- 云存储：需要网络，与离线可用需求冲突

**Data Structure**:
```csharp
[Serializable]
public class PlayerData
{
    public Dictionary<string, int> HighScores;
    public Dictionary<string, string> GameProgress;
    public bool SoundEnabled;
    public bool MusicEnabled;
}
```

---

### 5. UI 适配方案

**Decision**: 使用 Canvas Scaler (Scale With Screen Size) + 安全区域适配

**Rationale**:
- 自动缩放：根据屏幕尺寸自动调整 UI 大小
- 安全区域：适配刘海屏、打孔屏等异形屏
- 现有方案：项目已有 CameraScaler，可复用逻辑

**Canvas Scaler Settings**:
- UI Scale Mode: Scale With Screen Size
- Reference Resolution: 1080 x 1920
- Screen Match Mode: Match Width Or Height
- Match: 0.5 (平衡宽高)

---

### 6. 主菜单设计

**Decision**: 卡片式布局，垂直滚动列表

**Rationale**:
- 直观：每个游戏一张卡片，包含图标、名称、简介
- 可扩展：支持动态添加游戏，无需修改布局
- 触摸友好：卡片大小适合手指点击

**Layout**:
```
┌─────────────────────────┐
│      PawzyPop           │  <- 标题
│    小游戏集合            │
├─────────────────────────┤
│  ┌───────────────────┐  │
│  │  🎮 消消乐        │  │  <- 游戏卡片
│  │  交换方块消除得分  │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │  ⚫ 五子棋        │  │
│  │  与AI对弈         │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │  🔜 更多游戏      │  │  <- 占位/敬请期待
│  │  敬请期待         │  │
│  └───────────────────┘  │
├─────────────────────────┤
│  ⚙️ 设置  |  ℹ️ 关于   │  <- 底部导航
└─────────────────────────┘
```

## 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 五子棋 AI 性能不足 | 决策超时 | 限制搜索深度，使用协程分帧计算 |
| 场景切换卡顿 | 用户体验差 | 添加加载界面，异步加载 |
| 内存占用过高 | 低端设备崩溃 | 资源按需加载，及时卸载 |

## 结论

所有技术方案已确定，无需进一步澄清。可进入 Phase 1 设计阶段。
